version: '3.8'

services:
  voicevox:
    image: voicevox/voicevox_engine:cpu-latest
    ports:
      - "127.0.0.1:50021:50021"
    restart: unless-stopped
    # コンテナのタイムゾーンを合わせる場合
    environment:
      - TZ=Asia/Tokyo
    # tty を true にしておくとログが見やすくなる場合があります
    tty: true
